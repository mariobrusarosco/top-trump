import App from "./App";
import { render } from "@testing-library/react";

describe("Button", () => {
  describe("when rendering", () => {
    it("renders the text", () => {
      render(<App />);
      expect(false).toBe(true);
    });
  });
});
